We have received a request for password renewal
for your access to the partner program via the address {$content.REMOTE_ADDRESS}.

Your new password for your access to the partner program of {$content.STORE_NAME} is as follows: {$content.NEWPASS}