Der Lagerbestand eines Artikels im Shop ist {if $PRODUCTS_QUANTITY < $STOCK_AMOUNT}unter{else}auf{/if} die Mindestmenge von {$STOCK_AMOUNT} Stück gefallen.


Artikel: {$PRODUCTS_NAME}{if !empty($PRODUCTS_MODEL)} ({$PRODUCTS_MODEL}){/if}
Lagerbestand: {$PRODUCTS_QUANTITY} 
Link: {$PRODUCTS_LINK}

{if !empty($PRODUCTS_ATTRIBUTES)}
{foreach name=aussen item=attr from=$PRODUCTS_ATTRIBUTES}
{$attr.OPTIONS_NAME}: {$attr.VALUES_NAME}
Lagerbestand: {$attr.STOCK} 

{/foreach}
{/if}